{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug a.out (GDB)",
            "type": "lldb-dap",
            "request": "launch",
            "program": "${workspaceFolder}/src/main",
            "args": [ "--unit-tests", "test.c" ],
            "cwd": "${workspaceFolder}",
        }
    ]
}

